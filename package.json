{
	"name": "marksync-for-confluence",
	"version": "0.5.2",
	"description": "CLI tool that synchronizes a Git-tracked Markdown corpus to Atlassian Confluence Cloud — deterministically, safely, and with a clear audit trail.",
	"type": "module",
	"license": "MIT",
	"author": "Juliusz Ćwiąkalski",
	"private": true,
	"engines": {
		"bun": "1.2.23"
	},
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"default": "./dist/index.js"
		}
	},
	"imports": {
		"#domain/*": "./src/domain/*.ts",
		"#app/*": "./src/app/*.ts",
		"#infra/*": "./src/infra/*.ts",
		"#shared/*": "./src/shared/*.ts",
		"#cli/output": "./src/cli/output/index.ts",
		"#cli/*": "./src/cli/*.ts",
		"#tests/_helpers/*": "./tests/_helpers/*.ts"
	},
	"scripts": {
		"lint": "biome lint .",
		"format": "biome format --write .",
		"format:check": "biome format .",
		"typecheck": "tsc --noEmit",
		"test": "bun test",
		"test:bdd": "bun -e \"const{existsSync}=require('fs');if(!existsSync('node_modules/@cucumber/cucumber')){console.log('[test:bdd] BDD runner (@cucumber/cucumber) wires in E5-S1 (MS2-E5); not installed yet — nothing to run.');process.exit(0)}const{execFileSync}=require('child_process');execFileSync('bunx',['cucumber-js','tests/bdd/features'],{stdio:'inherit'})\"",
		"check:boundaries": "depcruise src",
		"check": "bun run lint && bun run format:check && bun run typecheck && bun run test && bun run check:boundaries",
		"prepare": "husky"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.2",
		"@commitlint/cli": "^21.2.0",
		"@commitlint/config-conventional": "^21.2.0",
		"@types/bun": "^1.3.14",
		"@types/hast": "^3.0.5",
		"@types/mdast": "^4.0.4",
		"dependency-cruiser": "^18.0.0",
		"husky": "^9.1.7",
		"typescript": "^6.0.3"
	},
	"dependencies": {
		"@jsr/cliffy__command": "^1.2.1",
		"@jsr/cliffy__flags": "^1.2.1",
		"ajv": "^8.20.0",
		"picocolors": "^1.1.1",
		"remark": "^15.0.1",
		"remark-frontmatter": "^5.0.0",
		"remark-gfm": "^4.0.1",
		"remark-rehype": "^11.1.2",
		"uuid": "^14.0.1",
		"yaml": "^2.9.0",
		"zod": "^4.4.3"
	}
}
